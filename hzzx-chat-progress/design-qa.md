# 四角色真实 UI 与单行反馈验证

final result: blocked

## 视觉依据

- 来源：voicehub.commind.com/hzzx/mobile/ 的普通用户、展商、客商、媒体四角色首页；2026-09-20 本轮通过浏览器读取。
- 已查看四角色桌面 1280 × 720 与移动端 390 × 844 的源页面截图、DOM、字体、主题变量、入口及图片；原始截图由浏览器工具展示，未另存过程图片。
- 真实素材已保存至 `assets/`，未保留带鉴权信息的页面地址。字体使用源页面的系统字体栈。
- 用户追加截图用于约束反馈形态：在原“思考中”位置只显示当前一句状态，首个非空正文出现后完全替换，不保留过程入口。

## 阻塞与限制

- Browser Use URL policy 拒绝导航及读取用户已打开的本地 `file://` 原型页面。未绕过该限制。
- 未获得实现截图，不能进行同尺寸、同状态的并排或局部视觉比较；不以静态检查或 DOM 模拟测试替代视觉验收。
- 字体与文案、主题变量、素材来源已作源码核对；布局间距、实际字体渲染、图像裁切、响应式密度仍待浏览器验证。
- 当前修改仅为本地原型，未部署、未提交；源站业务功能与权限系统未接入。

## 已完成的非视觉验证

- DOM 模拟环境执行实际页面脚本，173 项断言通过，无运行时错误。
- 验证 4 角色入口数量分别为 8、9、6、7；角色切换同时更新文案、素材、推荐问题、快捷项、主题，并隔离模拟对话。
- 验证 13 个反馈场景；首段正文前显示单行状态，首段正文后状态与长等待提示移除，后续工具事件和停顿不会恢复。
- 验证无任何过程展开按钮、步骤列表或历史过程入口；失败保留正文，重提保留草稿。
- 验证工具结束不推断成功、重复通知不重置计时、终态不接受迟到内容。
- 验证素材文件存在、DOM 标识唯一、PRD.md 与内嵌文档逐字一致。

## 后续视觉验收

- 在可访问的本地预览环境，对四角色分别查看首页、输入、生成前状态、首段正文、失败反馈。
- 与源页面同尺寸比较字体换行、功能卡片/吉祥物位置、主题渐变、底部输入区及快捷项。
- 验证短屏滚动、长文阅读不抢滚动、分栏拖拽、文档下载与弹层定位。
- 首段正文出现后检查原状态行完全消失，且不存在过程交互入口。
